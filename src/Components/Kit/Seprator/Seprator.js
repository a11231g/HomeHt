import React, { Component } from 'react';
import {
    View,
} from 'react-native';
import styles from './SepratorStyle';

export default class Seprator extends Component {
    render() {
        return (
            <View style={styles.seprator} />
        );
    }
}
